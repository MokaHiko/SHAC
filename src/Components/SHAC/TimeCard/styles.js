import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles((theme) => ({
  root: {
    marginTop: "5vh",
    height: 'fit-content',
  },
  page: {
    height: "100vh",
  },
  tableHead: {
    fontWeight: "bold",
  },
  addItem: {},
}));
