import { Grid, Typography } from '@mui/material'
import React from 'react'

function Cana() {
	return (
		<Grid id="Cana">
			<Typography color="primary" variant="h1">Cana</Typography>
		</Grid>
	)
}

export default Cana
